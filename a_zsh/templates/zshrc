# add custom local fpath dir
fpath=( {{zsh_customdir}}/functions/ $fpath )

# now auto load any functions you can find.
for fdir in ${fpath[@]}; do
	if [[ -d $fdir ]]; then
		autoload $fdir/*(N:t) >/dev/null
	fi
done

for file in {{zsh_customdir}}/*.zsh; do
	source $file
done
